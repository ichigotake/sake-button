machine:
  node:
    version: 6.11.5
dependencies:
  override:
    - npm install -g firebase-tools
deployment:
  deploy:
    branch: master
    commands:
      - firebase deploy --project "$FIREBASE_PROJECT" --token "$FIREBASE_TOKEN"
